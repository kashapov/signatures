const appName = 'Signature List';

const btnCreateSignatureText = 'Create a signature';
const btnUseText = 'Use';

const filterSelectedText = 'Selected';
const filterDeleteText = 'Delete';

const textSentByAdmin = 'Sent by admin';
const textInstalledByUser = 'Installed by user';
const textLastEdited = 'Last edited';
const textYes = 'YES';
const textNo = 'NO';

export {
  appName,
  btnCreateSignatureText,
  btnUseText,
  filterSelectedText,
  filterDeleteText,
  textSentByAdmin,
  textInstalledByUser,
  textLastEdited,
  textYes,
  textNo,
};
